<?xml version="1.0" encoding="UTF-8"?>
<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form" xmlns:l="/lib/layout">
    <l:layout title="${it.displayName}">
        <l:side-panel>
            <l:tasks>
                <l:task icon="symbol-arrow-back-outline plugin-ionicons-api" href="${rootURL}/manage" title="${%Back to manage Jenkins}" />
            </l:tasks>
        </l:side-panel>
        <l:main-panel>
            <h1>${it.displayName}</h1>
            <p>Manage all your models from various providers here</p>
            <f:form action="configSubmit" method="post" name="config">
                <f:section title="${%Models Registry}">
                    <f:entry title="${%Registered Models}">
                        <f:hetero-list name="modelConfigurations" hasHeader="true" descriptors="${it.modelDescriptors}" items="${it.modelConfigurations}"
                                       addCaption="Add new Model" />
                    </f:entry>
                </f:section>
                <f:bottomButtonBar>
                    <f:submit value="Save Models" />
                </f:bottomButtonBar>
            </f:form>
        </l:main-panel>
    </l:layout>
</j:jelly>
