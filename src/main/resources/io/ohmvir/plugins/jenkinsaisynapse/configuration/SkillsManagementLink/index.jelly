<?xml version="1.0" encoding="UTF-8"?>
<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form" xmlns:l="/lib/layout">
    <l:layout title="${it.displayName}">
        <l:side-panel>
            <l:tasks>
                <l:task icon="symbol-arrow-back-outline plugin-ionicons-api" href="${rootURL}/manage" title="${%Back to manage Jenkins}" />
            </l:tasks>
        </l:side-panel>
        <l:main-panel>
            <h1>${it.displayName}</h1>
            <p>Manage all your agent skills here</p>
            <f:form action="configSubmit" method="post" name="config">
                <f:section title="${%Skills Registry}">
                    <f:entry title="${%Registered Skills}">
                        <f:hetero-list name="skillConfigurations" hasHeader="true" descriptors="${it.skillDescriptors}" items="${it.skills}"
                                       addCaption="Add new Skill" />
                    </f:entry>
                </f:section>
                <f:bottomButtonBar>
                    <f:submit value="Save Skills" />
                </f:bottomButtonBar>
            </f:form>
        </l:main-panel>
    </l:layout>
</j:jelly>
